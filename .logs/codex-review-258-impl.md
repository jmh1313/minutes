REVIEW ONLY — do NOT modify files, do NOT run cargo. Adversarial diff review of my IMPLEMENTATION of the #258 fix you reviewed earlier. Run `git diff` yourself to see all changes (working tree, branch release/v0.18.0). I went with the root-cause-contained scope you recommended.

## What I implemented
1. crates/core/src/pid.rs: new `PidFileState { Active(u32), LockedAlive, Inactive }` with `is_active()`/`pid()`, and `inspect_pid_file(path)`. It returns LockedAlive ONLY when `read_to_string` fails AND `is_lock_violation(err)` is true. `is_lock_violation` checks `raw_os_error()` == Some(32)|Some(33) on `#[cfg(windows)]`, false elsewhere. Stale dead-PID files still cleaned up. `check_pid_file` left untouched.
2. live_transcript.rs: `session_status` now uses `inspect_pid_file`; `derive_session_status` first param changed `Option<u32>` -> `PidFileState`; standalone_active = `is_active()`; pid = `lt_pid_state.pid()`. Liveness NOT gated on heartbeat freshness (your Medium #2). Sidecar-skip guard (run_sidecar_inner_mpsc) now uses `inspect_pid_file(&lt_pid).is_active()`.
3. Converted all readers of persistently-locked PID files (the create_pid_guard ones: live-transcript + worker) to inspect_pid_file: cli/main.rs recording-start block + stop path; tauri commands.rs cmd_stop_live_transcript; context.rs marker-preservation; palette_dispatch.rs LIVE flag; dictation.rs live-transcript conflict check; cli_setup.rs cli_recording_active update-guard; jobs.rs new `worker_active()` + both `current_worker_pid().is_some()` callers swapped.
4. Stop paths (cli + tauri): match is_active(); on Some(pid) do the old behavior (print pid + Unix SIGTERM), on LockedAlive (pid None) write the stop sentinel only (live loop polls it inline at live_transcript.rs:797) and skip the Unix kill. Poll for PID-file removal unchanged.
5. Tests: pid.rs (absent/live-self/stale-cleaned/corrupt + a held-guard test asserting is_active() on both platforms, LockedAlive on win / Active on unix). live_transcript.rs: LockedAlive+healthy, LockedAlive+STALE-heartbeat (regression guard for your #2), Active(pid), Inactive. Updated 9 existing call sites None->PidFileState::Inactive.

All green locally: pid tests 15/15, new standalone 4/4, sidecar 20/20, fmt clean, clippy clean on `--all --no-default-features` AND `-p minutes-core --features streaming --tests`.

## Attack these specifically
1. `is_lock_violation`: are 32/33 the right and sufficient Windows codes? Could a transient ERROR_SHARING_VIOLATION on a NON-locked file (e.g. AV scanner) make a dead session read as LockedAlive=alive? Is that acceptable vs the alternative?
2. Stop on Windows LockedAlive: I write the sentinel and poll for PID-file-removal up to 30s. The live loop checks `check_and_clear_sentinel()` inline each iteration, then on exit drops the PidGuard which (on Windows, `#[cfg(not(unix))]` branch) closes the handle then removes the file. Does anything block PID-file removal, or could the poll hang the full 30s? Any deadlock if the same in-app process calls cmd_stop while holding the guard? (Note: cmd_stop_live_transcript returns early via the in-app stop flag when live_transcript_active is set, so the external-PID branch is only hit for CLI-started sessions — confirm that's actually true.)
3. Did I leave any reader of a create_pid_guard-locked PID file still on check_pid_file? (live-transcript, worker, dashboard pids). Recording `pid::create()` and dictation `create_pid_file()` release the lock at function return — so check_recording / dict readers are safe and I left them. Verify that reasoning.
4. derive_session_status: with the param type change, is there any path where standalone_active is true but I now read stale stats incorrectly, or where pid=None breaks a consumer? Tauri cmd_live_transcript_status returns active = in_app_active || status.active and reads line_count/duration from status — confirm no consumer requires status.pid to be Some when active.
5. `current_worker_pid` is now internally unused (pub, kept). Acceptable or remove?
6. Any cross-platform `#[cfg]` mistakes, unused-var warnings under non-windows for `is_lock_violation`'s `let _ = err`, or feature-gating gaps (e.g. the new live_transcript tests gated on whisper+streaming like their neighbors)?

Give me your sharpest objections. No edits.
